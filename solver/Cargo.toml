[package]
name = "pickomino-solver"
version = "0.2.0"
edition = "2024"

[lib]
name = "pickomino_solver"
crate-type = ["rlib"]

[dependencies]
itertools = "0.14.0"
serde = { version = "1.0.228", features = ["derive"] }

[[bench]]
name = "solver"
harness = false
